# Core web
fastapi
uvicorn[standard]
python-multipart

# ML stack — pinned to exactly what works with runpod/pytorch:2.4.0 base
torch==2.4.0
torchvision==0.19.0
ultralytics>=8.2.0
segment-anything==1.0
simple-lama-inpainting==0.1.1
opencv-python-headless==4.10.0.84    # or 4.9.0.80 — both fine
numpy

# Optional but harmless
certifi
